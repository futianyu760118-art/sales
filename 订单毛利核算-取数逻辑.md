# 订单毛利核算 — 取数逻辑与来龙去脉

---

## 一、数据源全景

```
┌──────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  外部 ERP     │     │  本地 JSON 数据库  │     │  前端 localStorage │
│ 192.168.0.127 │     │ database/*.json   │     │ oa_productRates   │
└──────┬───────┘     └────────┬─────────┘     └────────┬─────────┘
       │                      │                         │
       │ 同步                  │ 读取                     │ 读写
       ▼                      ▼                         │
┌──────────────┐     ┌──────────────────┐               │
│ orders.json  │     │ bom_items.json   │               │
│ order_products│    │ materials.json   │               │
│ customers    │     │ order_analysis   │◄──────────────┘
└──────────────┘     └──────────────────┘
```

### 各数据文件职责

| 文件 | 记录数 | 来源 | 职责 |
|------|--------|------|------|
| `orders.json` | 3,099 | ERP `orders.list` 同步 | 订单主表（订单号、客户、数量、金额、状态） |
| `order_products.json` | 7,411 | ERP `order_details.list` 同步 | 订单产品明细（每订单含哪些产品、各产品 bom_no 和数量） |
| `bom_items.json` | 167,950 | ERP `bom_details.list` 同步 + Excel 手动导入 | BOM 明细（每个产品的物料清单、层级、用量、单价、人工字段） |
| `materials.json` | 23,845 | ERP `materials` basicdata 接口 | 物料主数据（物料编码、名称、当前单价） |
| `order_analysis.json` | 1,922 | 系统自动创建 + 用户操作 | 订单分析卡片（审核状态、负责人、成品工价 product_rates） |

---

## 二、ERP 接口与同步

### 2.1 已授权接口

| 接口 | 路径 | 数据 | 状态 |
|------|------|------|------|
| 物料主数据 | `/api/v1/basicdata/data-supply/data/materials` | 23,845 条物料（含单价） | ✅ 200 |
| BOM 头表 | `/api/v1/external/boms.list` | BOM 编号 → bom_id 映射 | ✅ 200 |
| BOM 明细 | `/api/v1/external/bom_details.list` | 按 bom_id 查询子件列表 | ✅ 200 |
| 订单明细 | `/api/v1/external/order_details.list` | 按 order_no 查询订单产品 | ✅ 200 |

### 2.2 未授权接口（403）

| 接口 | 用途 | 状态 |
|------|------|------|
| `products` | 产品主数据 | ❌ 403 |
| `orders` | 订单列表 | ❌ 403 |
| `customers` | 客户主数据 | ❌ 403 |
| `labor_rates` | 工价审核表 | ❌ 403 |
| `wages` | 工价数据 | ❌ 403 |

### 2.3 同步流程

```
用户点击「同步订单」
  │
  ├─ 1. 调 ERP orders.list → 获取全部订单（分页200/页）
  │    └─ 过滤：HJ2 开头（排除样品 HJY）
  │    └─ 写入 orders.json（按 order_no + line_no 去重 upsert）
  │
  ├─ 2. 调 ERP order_details.list → 获取订单产品明细
  │    └─ 按 order_no 匹配，补充 product_code / bom_no
  │    └─ 写入 order_products.json
  │
  ├─ 3. 调 ERP boms.list + bom_details.list → 获取 BOM 数据
  │    └─ 按 bom_id 查询明细行
  │    └─ 写入 bom_items.json（source=external_sync）
  │
  └─ 4. 调 ERP materials → 获取物料主数据
       └─ 更新 materials.json 中的 unit_price（当前单价）
```

---

## 三、取数链路（从原始数据到毛利数字）

### 3.1 总链路图

```
用户打开订单
  │
  ├─ 列表接口 GET /api/order-analysis?page=1
  │    │
  │    ├─ 读取 orders.json → 订单基本信息（order_no, quantity, order_amount）
  │    ├─ 读取 order_analysis.json → 审核状态、product_rates（成品工价）
  │    ├─ 读取 bom_items.json + materials.json → _computeOrderPlanCost()
  │    │    │
  │    │    ├─ 读取 order_products.json → 该订单有哪些产品（bom_no + quantity）
  │    │    │
  │    │    ├─ 按 product_code + bom_no 合并数量（主单+样件=总量）
  │    │    │
  │    │    ├─ 对每个产品调 buildBomTree(productCode, qty, priceMap)
  │    │    │    │
  │    │    │    ├─ ① getBomLines(productCode) → 从 bom_items.json 取该产品的 BOM 明细
  │    │    │    │    └─ 5分钟缓存（getBomIndex）
  │    │    │    │
  │    │    │    ├─ ② 按 level 深度用栈算法构建树（.1 → ..2 → ...3）
  │    │    │    │
  │    │    │    ├─ ③ _mergeDupNodes() → 合并重复行（同 depth+code）
  │    │    │    │
  │    │    │    ├─ ④ SPC 多级展开 → 子件 material_code 在 bom_items 中有记录？
  │    │    │    │    └─ 有 → 展开（BFS，防环，限深20）
  │    │    │    │    └─ 无 → 叶子节点
  │    │    │    │
  │    │    │    ├─ ⑤ lineCostPerUnit(line, priceMap)
  │    │    │    │    ├─ 单价 = materials.json 的 unit_price（物料库当前价）
  │    │    │    │    ├─ 物料成本 = 单价 × BOM用量
  │    │    │    │    ├─ 人工 = (direct_labor + outsource_labor + processing_fee) × BOM用量
  │    │    │    │    └─ 费用 = (variable_overhead + fixed_overhead) × BOM用量
  │    │    │    │
  │    │    │    ├─ ⑥ rollup() → 递归计算各节点成本
  │    │    │    │    ├─ total_qty = BOM用量 × 订单数量（纯连乘）
  │    │    │    │    ├─ material_amount = 单价 × BOM用量 × 订单数量
  │    │    │    │    ├─ 有子件：material_amount = Σ children
  │    │    │    │    └─ 叶子：material_amount = 自身计算值
  │    │    │    │
  │    │    │    └─ ⑦ 产品汇总 = Σ 根节点 material_amount
  │    │    │
  │    │    ├─ Σ 所有产品 = BOM总成本（material + labor + expense）
  │    │    │
  │    │    ├─ 读取 order_analysis.product_rates → 成品工价
  │    │    │    ├─ 工价合计 = Σ (rate × qty) per product
  │    │    │    └─ 费用合计 = 工价合计 × 0.25
  │    │    │
  │    │    └─ plan_cost = {
  │    │         material: BOM物料 + 工价合计,
  │    │         labor: BOM人工,
  │    │         expense: BOM费用 + 费用合计,
  │    │         total: 三者之和
  │    │       }
  │    │
  │    └─ 返回: plan_total_cost, gross_profit, gross_rate
  │
  └─ 明细接口 GET /api/order-analysis/:id/line-items
       │
       ├─ 读取 order_products.json → 产品列表（bom_no, quantity）
       ├─ _mergeLocalProducts() → 按 bom_no 合并数量（主单+样件）
       ├─ _attachBomTrees() → 对每个产品构建 BOM 树（同 buildBomTree 逻辑）
       │    └─ 候选编码：bom_no 优先（跳过通用码如 3.1.FJ）
       │    └─ bestTree：有层级的优先，扁平的选有自制件的
       ├─ 读取 order_analysis.product_rates → 写入 labor_rate 字段
       └─ 返回: products[]（含 tree, material, labor, total, labor_rate）
```

### 3.2 单价取数逻辑

```
物料单价取值优先级：
  ① materials.json 的 unit_price（物料库当前价）← 最优先
  ② bom_items.json 的 unit_price（BOM 自带价）← 回退
  ③ 0（无价）← 标记为「无价」
```

### 3.3 数量取数逻辑

```
订单数量：
  来源：orders.json 的 quantity 字段
  合并：order_products.json 中同 bom_no 的多行数量累加（主单+样件）

BOM 用量：
  来源：bom_items.json 的 quantity 字段
  含义：整灯用量（不论层级深度，280g 铝锭 = 每盏灯用 280g）

订单总量：
  计算：BOM用量 × 订单数量（纯连乘，不除 base_qty）
  示例：280 × 20006 = 5,601,680g
```

### 3.4 成品工价取数逻辑

```
来源优先级：
  ① 后端 order_analysis.product_rates（JSON 持久化，跨账号共享）
  ② 前端 localStorage('oa_productRates')（页面刷新恢复）
  ③ ERP labor_rates.list（需授权，当前 403）

格式：{ "HJ-8110-709-01": 5.37 }
含义：每个产品的单灯工价

合计计算：
  工价合计 = Σ (单灯工价 × 产品数量) per product
  费用合计 = 工价合计 × 0.25
```

---

## 四、成本计算公式

### 4.1 单行物料成本（lineCostPerUnit）

```
输入：bom_items.json 的一行 + materials.json 的单价
输出：{ mat, lab, exp }

mat（物料）= 单价 × BOM用量
  单价 = materials.unit_price（当前价优先）或 bom_items.unit_price

lab（人工）= (direct_labor + outsource_labor + processing_fee) × BOM用量
  来源：bom_items.json 的 direct_labor / outsource_labor / processing_fee 字段
  注：仅 dot-format BOM 数据有这些字段，numeric-format 全为 0

exp（费用）= (variable_overhead + fixed_overhead) × BOM用量
  来源：bom_items.json 的 variable_overhead / fixed_overhead 字段
```

### 4.2 节点成本（rollup）

```
叶子节点（外购原材料，无子件）：
  material_amount = mat × 订单数量
  labor_amount = lab × 订单数量
  expense_amount = exp × 订单数量

有子件的节点（自制件/委外件）：
  material_amount = Σ children.material_amount（子件合计）
  labor_amount = 自身 lab × 订单数量（保留加工费等）
  expense_amount = 自身 exp × 订单数量

所有层级统一用 orderQty 作为乘数（BOM用量是整灯用量）。
```

### 4.3 产品成本（buildBomTree 返回值）

```
material = Σ 根节点 material_amount
labor = Σ 根节点 labor_amount
expense = Σ 根节点 expense_amount
total = material + labor + expense
```

### 4.4 计划成本（_computeOrderPlanCost 返回值）

```
BOM物料 = Σ 产品 material
BOM人工 = Σ 产品 labor
BOM费用 = Σ 产品 expense

工价合计 = Σ (product_rates[bom_no] × product.quantity)
费用合计 = 工价合计 × 0.25

plan_cost = {
  material: BOM物料 + 工价合计,
  labor: BOM人工,
  expense: BOM费用 + 费用合计,
  total: material + labor + expense
}
```

### 4.5 毛利计算

```
毛利润 = 订单金额 - plan_cost.total
毛利率 = 毛利润 / 订单金额 × 100%
```

---

## 五、完整示例（HJ202601-0020-0127）

### 5.1 原始数据

```
订单：orders.json id=129
  order_no = HJ202601-0020-0127
  quantity = 20006
  order_amount = 2,110,000

产品：order_products.json（4行，同 bom_no 合并）
  bom_no = HJ-8110-709-01
  merged quantity = 1+3+2+20000 = 20006

工价：order_analysis.json product_rates
  { "HJ-8110-709-01": 5.37 }
```

### 5.2 BOM 取数

```
候选编码：bom_no "HJ-8110-709-01" → 47行（numeric, 扁平）
         product_code "3.1.HJ-8110-001" → 158行（dot-format, 有层级）

选择：dot-format（有层级 + 有自制件）→ buildBomTree("3.1.HJ-8110-001", 20006, priceMap)

BOM 树构建：
  158行 → 去重(_mergeDupNodes) → 109行
  → SPC多级展开（自制件 material_code 查 bom_items）
  → 最终树：22根节点，20个有子件

单行计算（示例：铝锭 LD-05）：
  materials.json unit_price = 0.0231（物料库当前价）
  bom_items.json quantity = 280（整灯用量280g）
  material_amount = 0.0231 × 280 × 20006 = 129,399
```

### 5.3 成本汇总

```
BOM 成本：
  material = Σ 根节点 material_amount = 1,541,635
  labor = Σ 根节点 labor_amount = 321,092
  expense = Σ 根节点 expense_amount = 0
  BOM 合计 = 1,862,727

成品工价：
  rate = 5.37/盏
  工价合计 = 5.37 × 20006 = 107,432
  费用合计 = 107,432 × 0.25 = 26,858

plan_cost：
  material = 1,541,635 + 107,432 = 1,649,067
  labor = 321,092
  expense = 0 + 26,858 = 26,858
  total = 1,649,067 + 321,092 + 26,858 = 1,997,017
```

### 5.4 毛利

```
订单金额 = 2,110,000
计划成本 = 1,997,017
毛利润 = 2,110,000 - 1,997,017 = 112,983
毛利率 = 112,983 / 2,110,000 = 5.35%
```

---

## 六、三视图数据对照

| 视图 | 接口 | plan_total | gross_profit | gross_rate | 数据来源 |
|------|------|------------|---------------|------------|----------|
| 列表 | `GET /api/order-analysis` | 1,997,017 | 112,983 | 5.35% | _computeOrderPlanCost |
| 详情 | `GET /api/order-analysis/:id` | 1,997,017 | — | — | plan_cost 字段 |
| 明细 | `GET /api/order-analysis/:id/line-items` | 1,862,727(BOM) + 107,432(工价) + 26,858(费用) = 1,997,017 | — | — | _attachBomTrees + product_rates |

差异说明：明细展开的 BOM total（1,862,727）不含工价，工价在合计行单独显示（工价×订单量 + 费用×订单量），总计 = BOM + 工价合计 + 费用合计 = 1,997,017。

---

## 七、BOM 数据层级结构

### 7.1 层级表示方式

BOM 数据统一采用 **dot-format 层级编码**（`.1` / `..2` / `...3` / `....4`），表示父子树状结构：

```
.1     → 第1层（根级组件，直接组成成品）
..2    → 第2层（子组件，挂在第1层下面）
...3   → 第3层（孙组件，挂在第2层下面）
....4  → 第4层（原材料，挂在第3层下面）
```

示例（HJ-8110 灯具）：
```
.1  2.3.1.HJ-8110-01-001     [自制] 铁支架成品
..2     1.3.1.HJ-8110-01      [自制] 铁支架毛坯（成型）
...3        1.3.1.LD-05       [外购] 铝锭 ADC12（原材料）
..2     4.9.SF-04             [外购] 热缩管（辅料）
.1  2.6.3.LA28X...C-1V0       [自制] 光源板（LED灯珠+焊锡）
..2     1.6.3.K2835...        [外购] LED灯珠
..2     1.6.4.RJ.SM688...     [外购] 焊锡膏
.1  2.6.3.LA28X...Z-1V0       [自制] 驱动板（电子元件）
..2     1.6.4.C.1.0805...     [外购] 电容
..2     1.6.4.IC.1.ESOP16...  [外购] 驱动IC
..2     1.6.4.R.1.1206J...    [外购] 贴片电阻
```

### 7.2 物料属性与层级关系

| 属性 | 含义 | 在树中的位置 | 成本计算 |
|------|------|-------------|----------|
| 自制 | 自己生产的半成品/成品 | 父节点（有子件） | material = Σ children |
| 委外加工 | 外协加工的部件 | 父节点（有子件） | material = Σ children |
| 外购 | 外部采购的标准件/原料 | 叶子节点（无子件） | material = 单价 × 用量 × 订单量 |

### 7.3 数据来源

| 来源 | 层级格式 | 人工字段 | 物料属性 | 说明 |
|------|----------|----------|----------|------|
| Excel 手动导入 | dot-format（有层级） | direct_labor / processing_fee 等 | 外购/自制/委外 | 含完整成本字段 |
| ERP 外部同步 | dot-format（有层级） | 无（全为0） | 外购/自制/委外 | 按层级导入，属性已转换 |
| ERP 自动转换 | dot-format（自动生成） | 无 | 按 material_code 前缀推断（2.x→自制） | 从 numeric 格式自动转换 |

### 7.4 树状结构构建算法

系统采用 **SPC 多级 BOM 组装算法**（BFS 逐层展开）：

```
步骤1：读取该产品的 BOM 明细（bom_items.json 中 product_code 匹配）
步骤2：按 level 深度（.1 → ..2 → ...3）用栈算法构建父子树
步骤3：_mergeDupNodes 合并重复节点（同 depth+code 合并子件）
步骤4：SPC 多级展开 — 对每个子件检查：
  └─ 该子件的 material_code 在 bom_items 中是否存在 product_code 记录？
     ├─ 存在 → 该子件有自己的 BOM，展开为 children（递归，防环+限深20）
     └─ 不存在 → 叶子节点，停止展开
步骤5：rollup 递归计算成本（纯连乘，不除 base_qty）
```

### 7.5 bestTree 选择逻辑

一个产品可能有多个候选编码（bom_no 和 product_code），各自对应不同格式的 BOM 数据：

```
候选编码优先级：bom_no → product_code → 去前缀

选择规则：
  ① 跳过通用码（如 3.1.FJ，含其他产品错误数据）
  ② 优先选有层级的树（有子件的）
  ③ 同扁平时，优先选有自制件的（dot-format 优于 numeric）
```
