# 订单毛利核算流程（标准操作手册）

## 一、总体公式

```
毛利润 = 订单金额 - 订单总成本
订单总成本 = BOM成本 + 成品工价合计 + 费用合计
毛利率 = 毛利润 / 订单金额 × 100%
```

---

## 二、成本构成

### 1. BOM成本（物料+人工+费用）

**数据来源：** BOM 明细表 × 物料库当前单价

**计算规则：**
- 每个物料的 BOM 用量 = 整灯用量（不论层级深度）
- 订单总量 = BOM用量 × 订单数量
- 物料成本 = 单价 × BOM用量 × 订单数量
- 自制件成本 = 子件成本合计（父件 = Σ children）
- BOM人工 = direct_labor + outsource_labor + processing_fee（来自BOM数据）
- BOM费用 = variable_overhead + fixed_overhead（来自BOM数据）

**示例（HJ202601-0020-0127，数量20006盏）：**
```
BOM成本 = ¥1,862,727
  ├── 物料(material) = ¥1,541,635
  ├── 人工(labor)    = ¥321,092
  └── 费用(expense)  = ¥0
```

### 2. 成品工价

**数据来源：** 外部 ERP 工价审核表（按 bom_no 匹配）或手动导入

**计算规则：**
- 成品工价 = 单灯工价 × 订单数量
- 费用 = 成品工价 × 0.25

**配置方式：**
- 自动获取：ERP API `labor_rates.list`（需授权）
- 手动导入：订单明细表 → 「📥 导入工价」→ 粘贴 `BOM编码 [Tab] 工价金额`
- 手动编辑：订单明细表中直接编辑工价输入框
- 工价持久化：保存到 `order_analysis.product_rates`（后端 + localStorage 双保险）
- 跨账号共享：任何账号登录都能看到已保存的工价

**示例：**
```
成品工价 = ¥5.37/盏 × 20006盏 = ¥107,432
```

### 3. 费用

**计算规则：** 费用 = 成品工价 × 0.25

**示例：**
```
费用 = ¥107,432 × 0.25 = ¥26,858
```

---

## 三、完整计算示例

### 订单 HJ202601-0020-0127

| 项目 | 计算过程 | 金额 |
|------|----------|------|
| BOM成本 | 物料库单价 × BOM用量 × 订单量 | ¥1,862,727 |
| 成品工价 | 5.37/盏 × 20006盏 | ¥107,432 |
| 费用 | 107,432 × 0.25 | ¥26,858 |
| **订单总成本** | 1,862,727 + 107,432 + 26,858 | **¥1,997,017** |
| 订单金额 | — | ¥2,110,000 |
| **毛利润** | 2,110,000 - 1,997,017 | **¥112,983** |
| **毛利率** | 112,983 / 2,110,000 × 100% | **5.35%** |

---

## 四、三视图一致性

| 视图 | 显示内容 | 数据来源 |
|------|----------|----------|
| **列表** | plan_total_cost, gross_profit, gross_rate | `_computeOrderPlanCost`（含工价+费用） |
| **详情** | plan_cost三层卡（物料/人工/费用） | 同上 |
| **明细展开** | 合计行（成本+工价+费用=总计+毛利） | line-items API + 前端 _productRates |

三处数值必须一致（差异 < ¥2 为正常舍入）。

---

## 五、BOM 层级展开规则（SPC 多级 BOM）

### 层级判定
- **层级靠 material_id 桥接**：子件的 material_code 在 bom_items 中是否存在 product_code 记录
- 存在 → 该子件有下一级 BOM，继续展开
- 不存在 → 叶子节点，停止展开
- **不用 parent_bom_id**（不可靠）

### 数量计算
```
订单总量 = BOM用量 × 订单数量（纯连乘，不除 base_qty）
不论层级深度，BOM用量始终代表整灯用量
```

### 父子成本关系
```
有子件的节点（自制件/委外件）：
  material_amount = Σ children material_amount（父件成本 = 子件合计）
  labor_amount = 自身人工（加工费等，保留）

叶子节点（外购原材料）：
  material_amount = 单价 × BOM用量 × 订单数量
```

### 防环限深
- visited 集合防止循环引用
- MAX_LEVEL = 20 防止无限递归

---

## 六、成品工价管理

### 导入工价
1. 展开订单明细表
2. 点击「📥 导入工价」
3. 从 Excel 复制两列数据：`BOM编码 [Tab] 工价金额`
4. 粘贴到文本框 → 点击「应用」
5. 系统自动：保存到后端 + 计算费用(×0.25) + 更新毛利

### 编辑工价
- 在明细表中直接编辑工价输入框
- 费用和毛利实时更新
- 1秒后自动保存到后端（防抖）

### 工价持久化
| 存储 | 位置 | 用途 |
|------|------|------|
| 后端 | `order_analysis.product_rates` (JSON) | 跨账号共享、plan_cost 计算 |
| 前端 | `localStorage('oa_productRates')` | 页面刷新后快速恢复 |

### ERP 自动获取（需授权）
- 接口：`GET /:id/labor-rates` 或 line-items 内嵌
- 按 bom_no 匹配
- 当前 ERP 未授权 `labor_rates` 数据类型，需联系管理员开通

---

## 七、检查分析

### 一键检查（批量）
- 范围：当前页或全部订单
- 内容：基本信息、产品信息、数量异常、BOM缺失、物料库缺失、价格缺失、成本异常、利润预警
- 责任部门：2开头物料 → 工程部，其他 → 采购部
- 物料名称/规格：检查结果中包含物料编码 + 名称 + 规格

### 单订单检查
- 展开订单 → 点击「🔍 检查分析」
- 发现问题清单（含级别、分类、描述、责任人、整改建议）
- 支持导出（Excel）和通知（IM群聊）

---

## 八、审核流程

### 审核弹窗
- 显示实时 plan_cost（含工价+费用）
- 毛利率 = (订单金额 - plan_cost) / 订单金额

### 审核操作
| 操作 | 说明 |
|------|------|
| 提交审核 | 核算计划成本 + 生成快照 |
| 通过 | 订单标记为已通过 |
| 驳回 | 退回修改 |
| 重置 | 回到未提交状态 |

---

## 九、数据同步

### 订单同步
- 来源：ERP `orders.list` + `order_details.list`
- 过滤：HJ2 开头（排除样品 HJY）
- 自动：同步订单 + 产品信息 + order_products + 客户 + 产品库

### BOM 同步
- 来源：ERP `bom_details.list`（扁平数据）
- 本地组装：按 SPC 指南 BFS 算法构建多级树
- 增量同步：`updated_after` 参数

### 物料库同步
- 来源：ERP `materials`（basicdata 接口）
- 单价：物料库当前价优先，BOM单价回退

---

## 十、关键文件

| 文件 | 说明 |
|------|------|
| `backend/routes/order-analysis.js` | 核心核算逻辑（buildBomTree, _computeOrderPlanCost, line-items） |
| `frontend/order-analysis.html` | 前端展示（明细表、工价编辑、检查分析、导出） |
| `database/order_analysis.json` | 订单分析卡片（含 product_rates 工价持久化） |
| `database/bom_items.json` | BOM 明细数据（16万+行） |
| `database/order_products.json` | 订单产品明细 |
| `database/materials.json` | 物料库（含当前单价） |
